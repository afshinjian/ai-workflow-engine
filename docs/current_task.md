# Current Task

Mirror of `docs/TASK_QUEUE.md`'s `Current` set. Must contain exactly the same task ID(s) at the
same status as the task queue — `workflowctl check-task-state` fails otherwise.

There is no active `Current` task. AUTO-002 was accepted by the Human Owner and closed to `Done`
on 2026-07-27. AUTO-003 and every other remaining task stay `Planned` until separately
authorized.
