# Current Task

Mirror of `docs/TASK_QUEUE.md`'s `Current` set. Must contain exactly the same task ID(s) at the
same status as the task queue — `workflowctl check-task-state` fails otherwise.

There is no `Current` task at this moment: Stage 0 of `docs/MASTER_ROADMAP.md` (T-101..T-103)
closed on 2026-07-17. Next, per the roadmap and the human's explicit approval: one local git
commit preserving the validated working tree (no push, no remote branch), then `T-301`
(Milestone 3 normative architecture plan) is promoted to `Current`.
