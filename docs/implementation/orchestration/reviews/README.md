# Independent Review Evidence

Store immutable YAML as `<stage-id>/<review-id>.yaml`. A review names the exact
implementation commit, reviewer identity, rerun commands, findings and verdict.
The reviewer must differ from the implementation/remediation actor.

