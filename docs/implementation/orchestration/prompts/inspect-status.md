# Prompt: Inspect orchestration feature status read-only

Work in `/home/afshin-jian/ai-workflow-engine`. Perform no writes, commits,
pushes, migrations, lock breaking or repairs.

Read repository governance and all index/state/protocol files under
`docs/implementation/orchestration/`. Validate the state schema and semantic
rules, inspect branch/HEAD/worktree/concurrency signals, and verify referenced
stage, evidence, review, handoff and migration files exist. Derive prerequisite
closure and the unique next legal action.

Report: package publication status; current/candidate/next stage; completed,
rejected and blocked stages; HEAD/tree health; missing or stale evidence;
migration/schema status; unresolved risks; and the exact next continuation
prompt. Clearly label derived results. If inconsistent, report fail-closed
reasons; do not modify state to make it consistent.

