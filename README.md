# AI Workflow Engine

A reusable local orchestration engine for governed AI-assisted software development.

The engine coordinates:

- Codex as a read-only architect and reviewer
- OpenCode as a scoped implementation agent
- Git worktrees and repository inspection
- deterministic validation gates
- structured agent reports
- human approval gates
- controlled commit, push, and task-promotion workflows

Initial target integration:

- `/home/afshin-jian/amozesh_konkur`

This repository is independent from the projects it orchestrates.
